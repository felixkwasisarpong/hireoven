node_modules
.next
out
.env*.local
# Backup/copy variants too. `.env*.local` only matches names ENDING in `.local`,
# so `.env.local.bak`, `.env.local.20260820` and `.env.local.save` were all
# committable while holding production credentials.
.env*.local.*
.env.local.bak*
.vercel
.DS_Store

# Build caches and local script artifacts
tsconfig.tsbuildinfo
scripts/output/
.cache/


data.sql
schema.sql

# Playwright browser session captures (contain cookies / auth)
playwright-*-state.json

# Cursor internal
.claude/
