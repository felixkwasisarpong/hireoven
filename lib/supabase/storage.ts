/** Resume files: MinIO / S3-compatible storage (MINIO_* env). */
export {
  deleteResume,
  getResumeObject,
  getResumeUrl,
  isResumeStorageMinio,
  isResumeStorageS3,
  uploadResume,
} from "@/lib/storage/resume"
